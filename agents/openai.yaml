interface:
  display_name: "Panda"
  short_description: "Run Panda's config-driven Codex reviewer and optional Claude/OpenCode collaborator agents"
  default_prompt: "Use $panda, panda, ait, aiteam, or ai-team to get independent perspectives from Panda's saved behavior profile; without a config it starts with the Codex reviewer on gpt-5.5 medium, and one-off model-only runs should use a single named agent."
